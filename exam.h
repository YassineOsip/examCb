#ifndef EXAM_H
#define EXAM_H

#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int add(int a, int b); // LEVEL 1
int find_max(int num1, int num2); // LEVEL 2
char *reverse_string(char *str); // LEVEL 3
int is_prime(int number); // LEVEL 4
int is_palindrome(char *str); // LEVEL 5

#endif